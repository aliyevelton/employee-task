@using NetTask.Models
@model List<Employee>

<h1>Employee page</h1>
<ul>
    @foreach (var employee in Model)
    {
        <li>@employee.Id - Name: @employee.Name - Age: @employee.Age - Salary: @employee.Salary</li>
    } 
</ul>